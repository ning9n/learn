{
  "dev": {
    "user_host": "localhost:8083",
    "auth_host": "localhost:8888",
    "gateway_host": "localhost:8080",
    "production_host":"localhost:8084"
  },
  "gateway": {
    "user_host": "localhost:8080",
    "gateway_host": "localhost:8080",
    "production_host":"localhost:8080"
  }

}